# 普通用户个人档案采用轻量风险因素拆分

系统将普通用户个人档案轻量拆分为 `user_profiles` 和 `user_risk_factor_profiles`：前者保存展示资料与诊断反馈，后者保存进入 7 天风险预测的风险因素档案。这个决策没有继续拆出独立的诊断反馈表，避免档案结构过散；同时也不把所有健康字段继续压在 `user_profiles` 中，避免诊断反馈被误用为预测输入，并让训练数据导出的标签来源与预测特征来源保持清晰边界。

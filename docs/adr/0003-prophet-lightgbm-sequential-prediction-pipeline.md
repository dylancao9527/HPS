# 采用 Prophet 与 LightGBM 串联的双模型预测链路

系统采用 Prophet 与 LightGBM 串联的双模型预测引擎：先用 Prophet 将用户历史血压记录转换为未来7天预测期血压特征，再由 LightGBM 结合用户个人风险因素输出高血压发病风险概率。这个决策让系统不只基于最近一次血压做静态分类，而是把短期血压趋势纳入风险评估，也与论文题目“基于Prophet与LightGBM的高血压风险预测系统设计与实现”保持一致。

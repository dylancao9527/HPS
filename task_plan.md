# 论文补强与结构优化计划

## 目标

围绕《基于Prophet与LightGBM的高血压风险预测系统设计与实现》补强论文的模型主线，把重心从“系统功能介绍”进一步收束到 Prophet 血压趋势预测、LightGBM 风险分类、趋势融合、对照实验和模型解释。先产出 Markdown 计划与写作话术，再进入正文补写和图表重绘。

## 当前阶段

阶段 9：按提交差异更新论文与 ER 图（complete）

## 阶段清单

| 阶段 | 状态 | 产物 |
|---|---|---|
| 1. 资料盘点与问题收敛 | complete | `findings.md`、`progress.md`、`lunwen-doc/论文补强与结构优化计划.md` |
| 2. 重新训练对照实验并提取写作依据 | complete | `baseline_result.json`、`experiment_result.json`、`comparison_report.md` |
| 3. 补写 Prophet 与 LightGBM 方法公式 | complete | 可直接并入主论文的 Markdown 正文 |
| 4. 补写 LightGBM 对照实验与调参解释 | complete | 表格、结果解释、答辩话术 |
| 5. 补写 Prophet 回测、优势与工程策略 | complete | Prophet 方法、回测、复用/重训策略 |
| 6. 重绘图表与 ER 图 | complete | Mermaid/PNG/SVG 图源与插入说明 |
| 7. 截图缺口确认与主论文合并 | complete | 截图清单、正文合并稿 |
| 8. 校验与收尾 | complete | 路径检查、渲染检查、最终说明 |
| 9. 按 `652df0572920c53945dfb670b4d1277ee63391a1..1fb193487b69562f1bfd0f5e8537b36368191560` 更新论文 | complete | 主论文 Markdown、论文材料说明、E-R 图 drawio 源 |

## 用户已确认

- 可以直接修改主论文 Markdown。
- E-R 图需要按实体 E-R 图重绘。
- 当前截图没有变化，可以沿用现有截图。
- LightGBM 对照实验需要重新训练生成数据。
- 公式编号采用章节编号，如 `(4-1)`、`(5-1)`。

## 本次执行结果

- LightGBM 对照实验已重新训练：最终论文采用 `f1` 阈值策略结果，`recall_priority` 作为保守对照。
- 主论文 Markdown 已补强第 2、3、4、5、6 章相关内容。
- `figure-4-5-core-er.mmd` 已重绘为实体 E-R 图，并导出 PNG/SVG。
- 当前截图无变化，沿用现有 6 张页面截图。

## 关键约束

- 论文主线必须围绕 Prophet + LightGBM 双模型预测链路，系统功能只作为落地支撑。
- 不把系统表述为临床诊断系统，不写“确诊概率”“医疗决策”等措辞。
- E-R 图要按实体 E-R 图表达，使用实体、关系、属性等图形元素，不只用表格或类图式字段清单。
- 先写 Markdown，再视用户确认进入 DOCX 更新。

## 待用户确认的问题

1. 是否允许我直接修改 `lunwen-doc/论文写作材料/基于Prophet与LightGBM的高血压风险预测系统设计与实现.md` 主论文稿？
2. E-R 图风格是否按“Chen 实体关系图”处理：矩形实体、菱形关系、椭圆属性？
3. 当前 6 张页面截图是否可继续使用，还是你准备重新截最新版 UI？
4. LightGBM 对照实验需要重新训练，不能只沿用旧报告。
5. 公式编号是否按章节编号，如 `(4-1)`、`(4-2)`、`(5-1)`？

## 风险与注意

- 如果要补真实参考文献引用，后续需要单独核验文献来源，不能凭空加引用。
- 现有 E-R 图 `figure-4-5-core-er.mmd` 可能偏流程/结构图，需要重绘为实体 E-R 图。
- 当前任务涉及论文正文，改动前应先保留主论文现有版本或生成补丁文档。

## 2026-05-11 本轮新增约束

- 只根据 `652df0572920c53945dfb670b4d1277ee63391a1` 到 `1fb193487b69562f1bfd0f5e8537b36368191560` 的真实代码变化更新论文事实。
- 本轮 E-R 图只生成/更新 `.drawio` 源文件，不转 PNG/SVG/PDF 图片。
- 不扩大到 DOCX 成稿更新，除非用户后续明确要求。
